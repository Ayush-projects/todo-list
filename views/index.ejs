<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
  
  <%- include('../partials/nav') %>
  
      <ul class="collection with-header">
        
       <% if(items.length>0) {%>
        
          <% items.forEach(element => { %>
           <li class="collection-item" ><div> <%=element.listItem %><a href="/delete/<%= element._id %>" id="delete-btn" class="secondary-content"><i class="material-icons" >delete</i></a> </div></li>
          <% }); %>
     
        
        
     <% } else{ %>
      <li class="collection-item"><div>No items</div></li>
     <% } %>
      </ul>
      
       
        
          
  

 
       
      </div>

      
</body>
</html>